# xinli-mental-health 项目测试指南

> 本文档用于在**有 Java 环境的机器**上首次运行并测试项目。
> 所有新功能均在 `dev` 分支，git clone 后记得切换到 `dev` 分支。

---

## 一、环境准备

| 工具 | 版本要求 |
|---|---|
| JDK | 17+ |
| Maven | 3.8+ |
| MySQL | 8.0+ |
| Node.js | 18+ |
| Git | 任意 |

---

## 二、数据库初始化

1. 创建数据库：
```sql
CREATE DATABASE xinli_mental DEFAULT CHARACTER SET utf8mb4;
```

2. 按顺序执行项目根目录下的 SQL 文件：
```
# 先执行原始建表文件
xinli-mental-health/mental_health.sql

# 再执行测评模块建表（dev 分支新增）
xinli-mental-health/assessment_module.sql
```

3. 修改后端数据库连接配置：
- 文件路径：`xinli-backend/src/main/resources/application.yml`
- 修改 `spring.datasource.url`、`username`、`password`

---

## 三、后端启动

```bash
cd xinli-backend
mvn spring-boot:run
# 或在 IDE 中直接运行 XinliApplication.java
```

默认端口：`8080`

---

## 四、核心接口测试（建议使用 Postman / Apifox）

### 4.1 用户注册 & 登录（获取 Token）

**注册：**
```
POST http://localhost:8080/api/auth/register
Body: { "phone": "13800000001", "password": "123456", "nickname": "测试用户" }
```

**登录：**
```
POST http://localhost:8080/api/auth/login
Body: { "phone": "13800000001", "password": "123456" }
Response: { "data": { "token": "eyJ..." } }
```
> 复制 token，后续所有需要登录的接口 Header 加：`Authorization: Bearer <token>`

---

### 4.2 心理测评模块测试

**① 查看问卷列表（无需登录）：**
```
GET http://localhost:8080/api/assessment/list
```

**② 查看问卷详情（无需登录）：**
```
GET http://localhost:8080/api/assessment/1
```
> 数据库中已有 SDS 抑郁自评量表示例数据（assessment_module.sql 中 INSERT 部分）

**③ 提交答卷（需要登录 Token）：**
```
POST http://localhost:8080/api/assessment/submit
Header: Authorization: Bearer <token>
Body:
{
  "assessmentId": 1,
  "answers": {
    "1": 3,
    "2": 5,
    "3": 7
  }
}
```
> answers 格式：key = questionId，value = optionId（从问卷详情接口获取）

**④ 查看历史记录（需要登录 Token）：**
```
GET http://localhost:8080/api/assessment/history
Header: Authorization: Bearer <token>
```

---

### 4.3 心理干预资源模块测试（dev 分支新增）

**① 查看资源列表（无需登录）：**
```
GET http://localhost:8080/api/resource/list?page=1&size=10
```

**② 按类型筛选（无需登录）：**
```
GET http://localhost:8080/api/resource/list?type=article
```
> type 可选值：`article`（文章）、`audio`（音频）、`video`（视频）

**③ 查看资源详情：**
```
GET http://localhost:8080/api/resource/{id}
```

---

### 4.4 管理端接口测试

**管理员登录：**
```
POST http://localhost:8080/api/admin/auth/login
Body: { "username": "admin", "password": "admin123" }
```

**Admin - 新增测评问卷：**
```
POST http://localhost:8080/api/admin/assessment
Header: Authorization: Bearer <admin_token>
Body: { "title": "GAD-7 焦虑症筛查", "description": "本量表用于评估广泛性焦虑障碍" }
```

**Admin - 发布干预资源：**
```
POST http://localhost:8080/api/admin/resource
Header: Authorization: Bearer <admin_token>
Body:
{
  "title": "正念呼吸练习",
  "type": "audio",
  "content": "...",
  "coverUrl": "...",
  "tags": "放松,冥想"
}
```

---

## 五、前端启动

### 用户端（xinli-user）
```bash
cd xinli-user
npm install
npm run dev
# 访问 http://localhost:5173
```

### 管理端（xinli-admin）
```bash
cd xinli-admin
npm install
npm run dev
# 访问 http://localhost:5174
```

> **注意**：前端请求代理配置在 `vite.config.ts` 的 `proxy` 配置中，默认代理到 `http://localhost:8080`

---

## 六、分支说明

| 分支 | 说明 |
|---|---|
| `main` | 稳定主分支，只合并测试通过的代码 |
| `dev` | 开发分支，所有新功能在此开发 |

**合并流程**：在有环境的机器上测试 `dev` 分支通过后，执行：
```bash
git checkout main
git merge dev
git push origin main
```

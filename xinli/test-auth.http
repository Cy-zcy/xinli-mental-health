### 测试管理员认证API（无需授权）

### 1. 管理员登录（无需授权）
POST http://localhost:8080/api/admin/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}

### 2. 管理员注册（无需授权，但功能未开放）
POST http://localhost:8080/api/admin/auth/register
Content-Type: application/json

{
  "username": "newadmin",
  "password": "newpassword"
}

### 3. 获取当前用户信息（需要token）
GET http://localhost:8080/api/admin/auth/me
Authorization: Bearer YOUR_TOKEN_HERE

### 4. 退出登录（需要token）
POST http://localhost:8080/api/admin/auth/logout
Authorization: Bearer YOUR_TOKEN_HERE

### 5. 测试需要认证的接口（需要token）
GET http://localhost:8080/api/admin/users?page=1&size=10
Authorization: Bearer YOUR_TOKEN_HERE
